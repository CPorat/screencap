# US-004 Context Snapshot

- task statement: Implement US-004 "Swift bridge for active window metadata" from `.ralph/prd.json`.
- desired outcome: Add a native bridge that returns frontmost app name, window title, and bundle ID to Rust; preserve `mock-capture` testability; pass required Rust quality checks.
- known facts/evidence:
  - Current branch is `ralph/v1-implementation`, matching PRD `branchName`.
  - Highest-priority remaining story is US-004 (`priority: 4`, `passes: false`).
  - `src/capture/window.rs` is currently empty.
  - Existing Swift bridge for screenshots already compiles sources from `swift/Sources/` via `build.rs` and exposes C ABI through `swift/Sources/bridge.h`.
  - `mock-capture` is already wired in `src/capture/screenshot.rs` and should gate the window-info mock too.
  - LSP is not configured for this workspace, so semantic reference lookup is unavailable; use structural/text search instead.
- constraints:
  - macOS only; no cross-platform abstractions.
  - Keep bridge logic minimal in Swift and policy/typing in Rust.
  - No network in capture layer.
  - Do not widen scope beyond US-004 except required bookkeeping/docs.
  - Required verification: `cargo check`, `cargo clippy`, `cargo test --features mock-capture`.
- unknowns/open questions:
  - `.factory/library/architecture.md` was requested by prompt but is absent from the repo.
  - Exact CGWindow filtering needed to map the frontmost app PID to a stable window title may need experimentation.
  - Whether any reusable codebase pattern emerges worth adding to `AGENTS.md`.
- likely codebase touchpoints:
  - `swift/Sources/bridge.h`
  - `swift/Sources/WindowInfo.swift` (new)
  - `src/capture/window.rs`
  - maybe `Cargo.toml` if additional crate features are required for tests
  - `.ralph/prd.json`
  - `.ralph/progress.txt`
  - `AGENTS.md`
  - `.pi/state/pi-ralph.json`
