# Context Snapshot — US-002 SQLite schema, migrations, and typed models

- Task statement: Implement the highest-priority unfinished PRD story, currently US-002 (SQLite schema, migrations, and typed models).
- Desired outcome: Add the storage schema and typed persistence layer described in SPEC.md, with tests and green Rust quality checks.
- Known facts/evidence:
  - PRD branchName is `ralph/v1-implementation`; current branch matches.
  - US-001 is complete; US-002 is the highest-priority story with `passes: false`.
  - AGENTS.md requires typed Rust structs, SQLite + FTS5, UTC timestamps, and testable config helpers.
  - `.ralph/progress.txt` already records one reusable pattern: separate config location from resolved config values.
  - `.factory/library/architecture.md` was requested by the task instructions but is not present in the repository.
- Constraints:
  - Implement only one story this iteration.
  - No network in capture layer; no cross-platform abstractions.
  - Use existing code patterns; run `cargo check`, `cargo clippy`, and `cargo test --features mock-capture` before completion.
  - Keep design coherent: no compatibility shims or duplicate representations.
- Unknowns/open questions:
  - Exact current storage module layout and whether existing stubs already imply naming or API boundaries.
  - Whether FTS triggers are expected by SPEC or if explicit updates from storage methods are sufficient.
- Likely codebase touchpoints:
  - `src/storage/mod.rs`
  - `src/storage/db.rs`
  - `src/storage/models.rs`
  - any storage re-exports in `src/lib.rs`
  - `Cargo.toml` if SQLite feature flags are missing
  - `.ralph/prd.json`, `.ralph/progress.txt`, `AGENTS.md`
