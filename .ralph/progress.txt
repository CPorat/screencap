# Ralph Progress Log
Started: 2026-04-10
Project: Screencap v1
Branch: ralph/v1-implementation

## Codebase Patterns
- Config loaders should separate where config lives from what config resolves to: accept explicit root/home inputs for tests, then create runtime directories from resolved storage and export paths after parsing.
- When FTS search text is assembled from multiple tables, use a standalone FTS index keyed by the primary row id and rebuild it from storage writes instead of relying on an external-content FTS table tied to a single base table.
- For Swift capture bridges, keep enumeration and capture on the same native backend, expose C-callable functions from `swift/Sources/`, and keep `mock-capture` JPEG generation in Rust so permissionless tests exercise real files.
- When the Swift bridge spans multiple source files, build the archive directly with `swiftc -emit-library -static` over all `swift/Sources/*.swift` inputs; `-emit-object -o` only works for a single Swift source.
- When resolving frontmost window titles from `CGWindowListCopyWindowInfo`, stop at the first layer-0 window for the active app PID; if it is untitled, propagate an empty string instead of falling through to another window from the same app.
- When one capture cycle spans multiple displays, stage every JPEG for that timestamp and persist the matching `captures` rows in one SQLite transaction; if any write fails, remove the new files so the screenshot tree and database cannot diverge.
- Daemon lifecycle should use a PID file under `~/.screencap/` that stores both `pid` and `started_at`; `start`, `stop`, and `status` should verify process liveness so stale PID files self-heal instead of blocking future runs.
- REST API read endpoints should open SQLite read-only and return empty/404 responses without creating `screencap.db`; read traffic must not mutate runtime state.
- Screenshot-serving endpoints should sanitize relative paths and walk them with `openat(..., O_NOFOLLOW)` so symlinks and traversal attempts cannot escape the screenshots root.
- OpenAI-compatible provider code should resolve default base URLs from the provider enum in one place and return structured response text plus optional token usage, so local backends that omit usage do not force fake cost data.
- LLM JSON parsers should tolerate optional markdown code fences from model output, but they should still validate invariants like unique capture IDs before persisting so downstream jobs can trust the parsed batch.




---

## 2026-04-10 18:58:40 UTC - US-001
- Implemented the initial Rust package skeleton: Cargo manifest, library/binary roots, clap CLI scaffold, daemon stub, and module directories for capture, pipeline, ai, storage, api, mcp, and export.
- Added typed TOML config parsing with SPEC defaults, provider enum parsing, home-path expansion, and runtime directory bootstrapping for storage, screenshots, and daily exports.
- Added unit tests covering missing config defaults, valid config parsing, and invalid TOML failures.
- Files changed: Cargo.toml, AGENTS.md, src/lib.rs, src/main.rs, src/config.rs, src/daemon.rs, src/capture/*, src/pipeline/*, src/ai/*, src/storage/*, src/api/*, src/mcp/*, src/export/*, .pi/context/us-001-foundation-20260410T000000Z.md, .pi/state/pi-ralph.json
- **Learnings for future iterations:**
  - Config loaders should accept explicit root/home inputs so tests avoid mutating global HOME state.
  - Create runtime directories from resolved config values after parsing, not from raw TOML strings.
  - Keep the binary thin: export shared modules from `lib.rs` and keep `main.rs` focused on clap parsing and command dispatch.
---
## 2026-04-10 19:26:47 UTC - US-002
- Implemented the SQLite storage layer with schema initialization, typed models for captures/extractions/batches/insights, CRUD helpers, and ranked FTS search over extraction content plus batch narrative.
- Added tests covering on-disk database creation, capture insert/query/status flows, extraction search ranking and punctuated queries, insight persistence/FTS sync, daily payload parsing, and invalid insight window rejection.
- Files changed: AGENTS.md, src/storage/db.rs, src/storage/models.rs, .ralph/prd.json, .ralph/progress.txt, .pi/state/pi-ralph.json
- **Learnings for future iterations:**
  - Store DB timestamps as fixed-width RFC3339 UTC strings with fractional seconds so lexical ordering and round-tripping stay correct at subsecond precision.
  - Derive denormalized insight narrative columns from the typed payload instead of accepting a second caller-provided copy that can drift.
  - Quote free-text FTS input before passing it to SQLite MATCH so punctuation-heavy queries behave like user search, not parser syntax.
---

## 2026-04-10 20:15:17 UTC - US-003
- Implemented the ScreenCaptureKit screenshot bridge: Cargo now builds a Swift static library from `swift/Sources/`, the C ABI exposes display enumeration plus screenshot capture, and the Rust wrapper validates JPEG quality, preserves filesystem paths, and surfaces bridge failures.
- Added a `mock-capture` fallback in Rust that emits real JPEG files and tests covering valid JPEG generation, display ID consistency, invalid display IDs, and invalid quality handling.
- Files changed: AGENTS.md, Cargo.toml, build.rs, src/capture/screenshot.rs, swift/Sources/ScreenCapture.swift, swift/Sources/bridge.h, .ralph/prd.json, .ralph/progress.txt, .pi/context/us-003-screenshot-20260410T192929Z.md, .pi/state/pi-ralph.json
- **Learnings for future iterations:**
  - When a Swift bridge exposes display identifiers, return the IDs from the same ScreenCaptureKit snapshot used for capture-related queries so hot-plug races do not create uncapturable IDs.
  - Keep filesystem paths in native bridges as file-system representations/URLs rather than forcing UTF-8 string conversion, because macOS paths are not guaranteed to be valid UTF-8.
  - Pass Cargo’s target architecture through to `swiftc` in `build.rs`; otherwise cross-architecture macOS builds produce a Swift object for the host arch and fail at link time.
---

## 2026-04-10 20:21:27 UTC - US-004
- Implemented the active-window metadata bridge: added a Swift C ABI that returns frontmost app name, window title, and bundle ID, plus a Rust `WindowInfo` wrapper that frees bridge-owned strings safely.
- Added a shared `free_bridge_string` bridge function, a Rust `mock-capture` fallback returning deterministic window metadata, and unit coverage for the mock path.
- Updated `build.rs` to archive multi-file Swift bridges with `swiftc -emit-library -static`, which keeps the bridge build working now that `swift/Sources/` has more than one Swift file.
- Files changed: AGENTS.md, build.rs, src/capture/screenshot.rs, src/capture/window.rs, swift/Sources/WindowInfo.swift, swift/Sources/bridge.h, .ralph/prd.json, .ralph/progress.txt, .pi/context/us-004-window-20260410T201731Z.md, .pi/state/pi-ralph.json
- **Learnings for future iterations:**
  - For Swift-to-Rust string outputs, allocate bridge-owned C strings in Swift and pair them with an explicit free function so Rust can copy into owned `String`s without allocator assumptions.
  - Window titles are optional at the OS level; normalize "missing title" to an empty string at the bridge boundary so downstream capture code can stay typed and branch-free.
  - `CGWindowListCopyWindowInfo` returns windows front-to-back, so the first layer-0 window for the active PID is the only truthful source for the current window title.
---

## 2026-04-10 20:41:14 UTC - US-005
- Implemented the timer-based capture loop in `src/daemon.rs`: it polls on `idle_interval_secs`, reads the active window, skips excluded contexts, deduplicates against the latest persisted capture, captures every display, and logs each cycle via tracing.
- Added transactional multi-display capture persistence in `src/storage/db.rs` with `insert_captures` plus `get_latest_capture`, so a capture cycle can compare against the last stored context and keep disk/database state aligned.
- Added mock-capture unit coverage for excluded-app skipping, dedup within the idle interval, and the `YYYY/MM/DD/HHMMSS-{display_id}.jpg` screenshot path convention.
- Files changed: AGENTS.md, src/daemon.rs, src/storage/db.rs, .ralph/prd.json, .ralph/progress.txt
- **Learnings for future iterations:**
  - Dedup should read from the latest persisted capture rather than only in-memory loop state so restarts preserve the same skip behavior.
  - Multi-display capture is one logical event; write its JPEGs first, then persist all corresponding capture rows in one transaction, cleaning up the files if persistence fails.
---

## 2026-04-10 20:53:00 UTC - US-006
- Implemented daemon lifecycle management: foreground runs now bind the localhost API listener, acquire a managed PID file, respond to SIGTERM/SIGINT, and background starts detach via `setsid` before spawning the internal daemon child.
- Added `start`, `stop`, and `status` CLI behavior with process liveness checks, stale PID cleanup, uptime/reporting output, and storage/capture metrics backed by SQLite plus filesystem traversal.
- Added minimal API server bootstrapping on `127.0.0.1:<port>` so the daemon now owns both the capture loop and HTTP listener before declaring startup complete.
- Added mock-capture coverage for stale PID cleanup and end-to-end CLI lifecycle tests covering both background and foreground daemon shutdown flows with per-test server ports.
- Files changed: AGENTS.md, Cargo.toml, src/api/routes.rs, src/api/server.rs, src/config.rs, src/daemon.rs, src/main.rs, src/storage/db.rs, tests/daemon_cli.rs, .ralph/prd.json, .ralph/progress.txt
- **Learnings for future iterations:**
  - PID files are more reliable if they store both `pid` and `started_at`; status/stop can then distinguish live daemons from stale on-disk state and recover automatically after crashes.
  - For lifecycle tests, foreground daemons are best verified through the real CLI `stop` path instead of ad hoc signals so shutdown behavior and PID-file cleanup stay covered together.
---
## 2026-04-10 22:12:14 UTC - US-007
- Implemented the axum REST surface for daemon health/stats plus read-only capture, screenshot, and app listing endpoints, including JSON error responses and screenshot URL derivation from stored capture paths.
- Added storage helpers and metrics support for filtered capture listing, capture detail lookup, app capture counts, and directory-size/captures-today reporting without creating the SQLite database on first read.
- Added API integration coverage that boots the server on a random localhost port, verifies empty-state reads, exercises capture/detail/apps/stats/screenshot endpoints against real fixtures, and confirms screenshot traversal/symlink escapes are rejected.
- Files changed: AGENTS.md, Cargo.toml, src/api/routes.rs, src/api/server.rs, src/daemon.rs, src/main.rs, src/storage/db.rs, src/storage/mod.rs, src/storage/models.rs, src/storage/metrics.rs, tests/api_server.rs, tests/daemon_cli.rs, .ralph/prd.json, .ralph/progress.txt
- **Learnings for future iterations:**
  - Read-only API endpoints should open SQLite read-only and return empty/404 responses without creating runtime state on disk.
  - Screenshot file serving should treat stored paths as untrusted input: sanitize them to relative components and walk the tree with no-follow opens so symlinks cannot escape the screenshots root.
---
## 2026-04-10 22:20:31 UTC - US-008
- Implemented the shared AI provider contract in `src/ai/provider.rs`, including provider config normalization, environment-based API key loading, default OpenAI-compatible base URL resolution, and a factory that routes OpenRouter/OpenAI/LM Studio through one client.
- Added `src/ai/openai_compat.rs` with an OpenAI chat-completions client that sends base64 JPEGs as data URLs, parses assistant text plus token-usage metadata, and classifies auth/rate-limit/network failures without inventing usage for providers that omit it.
- Added `src/ai/mock.rs` for reusable canned LLM responses and request recording, then covered the new provider stack with unit/integration tests for factory construction, base URL defaults, image payload encoding, usage parsing, and mock responses.
- Files changed: AGENTS.md, Cargo.toml, src/ai/mod.rs, src/ai/mock.rs, src/ai/openai_compat.rs, src/ai/provider.rs, .ralph/prd.json, .ralph/progress.txt
- **Learnings for future iterations:**
  - Keep token usage optional at the provider boundary; LM Studio-style local backends may not report usage, and coercing that to zero would lie to downstream cost tracking.
  - Resolve provider defaults such as `base_url` centrally in the provider layer so extraction and synthesis callers only pass config, not duplicated URL selection logic.
  - A reusable mock provider should record prompts and queued responses, so future pipeline tests can assert both the request shape and the parsed output without live network calls.
---
## 2026-04-10 22:26:14 UTC - US-009a
- Implemented the extraction prompt template from SPEC.md plus a typed extraction response parser that reuses the storage-layer `ActivityType` and `Sentiment` enums instead of inventing duplicate schema types.
- Added prompt construction that emits per-capture frame metadata for attached screenshots, plus parser validation for empty/duplicate frame IDs and unit coverage for prompt contents, golden JSON parsing, and malformed JSON failures.
- Files changed: AGENTS.md, src/pipeline/extraction.rs, src/pipeline/prompts.rs, .ralph/prd.json, .ralph/progress.txt
- **Learnings for future iterations:**
  - Reuse storage-layer enums in pipeline parsers so prompt/parse code and persistence share one schema representation.
  - Accept fenced JSON from LLMs at the parser boundary, then validate batch invariants before any scheduler writes to SQLite.
---