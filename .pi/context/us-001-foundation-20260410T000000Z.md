# Context Snapshot

- task statement: Implement the highest-priority unfinished story from `.ralph/prd.json`, which is US-001 Cargo workspace skeleton and config parsing.
- desired outcome: A compilable Rust project skeleton with clap CLI subcommands, TOML config parsing and defaults, `~/.screencap` directory bootstrapping, module stubs, and config tests.
- known facts/evidence:
  - Current branch is `ralph/v1-implementation`, matching PRD `branchName`.
  - Repository currently contains only `SPEC.md`, `AGENTS.md`, `.ralph/`, and `.agents/skills/...`; there is no existing `src/` or `Cargo.toml`.
  - `.factory/library/architecture.md` referenced by instructions is absent from the repo.
  - PRD marks US-001 as priority 1 with `passes: false`.
  - SPEC defines config sections `[capture]`, `[extraction]`, `[synthesis]`, screenshot path conventions, and module layout.
- constraints:
  - macOS-only design; no cross-platform abstractions.
  - Capture layer must never use the network.
  - Use TOML config at `~/.screencap/config.toml`.
  - Use typed Rust structs and standard cargo tooling.
  - Complete only US-001 in this iteration.
- unknowns/open questions:
  - No architecture supplement beyond SPEC is available.
  - Some later-story dependencies (Swift bridge, actual daemon behavior) are intentionally stubbed for now.
- likely codebase touchpoints:
  - `Cargo.toml`
  - `src/main.rs`
  - `src/config.rs`
  - `src/*/mod.rs` stubs for capture, pipeline, ai, storage, api, mcp, export
  - `.ralph/prd.json`
  - `.ralph/progress.txt`
  - `AGENTS.md`
