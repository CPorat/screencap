# Ralph Progress Log
Started: 2026-04-10
Project: Screencap v1
Branch: ralph/v1-implementation

## Codebase Patterns
- Config loaders should separate where config lives from what config resolves to: accept explicit root/home inputs for tests, then create runtime directories from resolved storage and export paths after parsing.
- When FTS search text is assembled from multiple tables, use a standalone FTS index keyed by the primary row id and rebuild it from storage writes instead of relying on an external-content FTS table tied to a single base table.
- For Swift capture bridges, keep enumeration and capture on the same native backend, expose C-callable functions from `swift/Sources/`, and keep `mock-capture` JPEG generation in Rust so permissionless tests exercise real files.
- When the Swift bridge spans multiple source files, build the archive directly with `swiftc -emit-library -static` over all `swift/Sources/*.swift` inputs; `-emit-object -o` only works for a single Swift source.
- When resolving frontmost window titles from `CGWindowListCopyWindowInfo`, stop at the first layer-0 window for the active app PID; if it is untitled, propagate an empty string instead of falling through to another window from the same app.


---

## 2026-04-10 18:58:40 UTC - US-001
- Implemented the initial Rust package skeleton: Cargo manifest, library/binary roots, clap CLI scaffold, daemon stub, and module directories for capture, pipeline, ai, storage, api, mcp, and export.
- Added typed TOML config parsing with SPEC defaults, provider enum parsing, home-path expansion, and runtime directory bootstrapping for storage, screenshots, and daily exports.
- Added unit tests covering missing config defaults, valid config parsing, and invalid TOML failures.
- Files changed: Cargo.toml, AGENTS.md, src/lib.rs, src/main.rs, src/config.rs, src/daemon.rs, src/capture/*, src/pipeline/*, src/ai/*, src/storage/*, src/api/*, src/mcp/*, src/export/*, .pi/context/us-001-foundation-20260410T000000Z.md, .pi/state/pi-ralph.json
- **Learnings for future iterations:**
  - Config loaders should accept explicit root/home inputs so tests avoid mutating global HOME state.
  - Create runtime directories from resolved config values after parsing, not from raw TOML strings.
  - Keep the binary thin: export shared modules from `lib.rs` and keep `main.rs` focused on clap parsing and command dispatch.
---
## 2026-04-10 19:26:47 UTC - US-002
- Implemented the SQLite storage layer with schema initialization, typed models for captures/extractions/batches/insights, CRUD helpers, and ranked FTS search over extraction content plus batch narrative.
- Added tests covering on-disk database creation, capture insert/query/status flows, extraction search ranking and punctuated queries, insight persistence/FTS sync, daily payload parsing, and invalid insight window rejection.
- Files changed: AGENTS.md, src/storage/db.rs, src/storage/models.rs, .ralph/prd.json, .ralph/progress.txt, .pi/state/pi-ralph.json
- **Learnings for future iterations:**
  - Store DB timestamps as fixed-width RFC3339 UTC strings with fractional seconds so lexical ordering and round-tripping stay correct at subsecond precision.
  - Derive denormalized insight narrative columns from the typed payload instead of accepting a second caller-provided copy that can drift.
  - Quote free-text FTS input before passing it to SQLite MATCH so punctuation-heavy queries behave like user search, not parser syntax.
---

## 2026-04-10 20:15:17 UTC - US-003
- Implemented the ScreenCaptureKit screenshot bridge: Cargo now builds a Swift static library from `swift/Sources/`, the C ABI exposes display enumeration plus screenshot capture, and the Rust wrapper validates JPEG quality, preserves filesystem paths, and surfaces bridge failures.
- Added a `mock-capture` fallback in Rust that emits real JPEG files and tests covering valid JPEG generation, display ID consistency, invalid display IDs, and invalid quality handling.
- Files changed: AGENTS.md, Cargo.toml, build.rs, src/capture/screenshot.rs, swift/Sources/ScreenCapture.swift, swift/Sources/bridge.h, .ralph/prd.json, .ralph/progress.txt, .pi/context/us-003-screenshot-20260410T192929Z.md, .pi/state/pi-ralph.json
- **Learnings for future iterations:**
  - When a Swift bridge exposes display identifiers, return the IDs from the same ScreenCaptureKit snapshot used for capture-related queries so hot-plug races do not create uncapturable IDs.
  - Keep filesystem paths in native bridges as file-system representations/URLs rather than forcing UTF-8 string conversion, because macOS paths are not guaranteed to be valid UTF-8.
  - Pass Cargo’s target architecture through to `swiftc` in `build.rs`; otherwise cross-architecture macOS builds produce a Swift object for the host arch and fail at link time.
---

## 2026-04-10 20:21:27 UTC - US-004
- Implemented the active-window metadata bridge: added a Swift C ABI that returns frontmost app name, window title, and bundle ID, plus a Rust `WindowInfo` wrapper that frees bridge-owned strings safely.
- Added a shared `free_bridge_string` bridge function, a Rust `mock-capture` fallback returning deterministic window metadata, and unit coverage for the mock path.
- Updated `build.rs` to archive multi-file Swift bridges with `swiftc -emit-library -static`, which keeps the bridge build working now that `swift/Sources/` has more than one Swift file.
- Files changed: AGENTS.md, build.rs, src/capture/screenshot.rs, src/capture/window.rs, swift/Sources/WindowInfo.swift, swift/Sources/bridge.h, .ralph/prd.json, .ralph/progress.txt, .pi/context/us-004-window-20260410T201731Z.md, .pi/state/pi-ralph.json
- **Learnings for future iterations:**
  - For Swift-to-Rust string outputs, allocate bridge-owned C strings in Swift and pair them with an explicit free function so Rust can copy into owned `String`s without allocator assumptions.
  - Window titles are optional at the OS level; normalize "missing title" to an empty string at the bridge boundary so downstream capture code can stay typed and branch-free.
  - `CGWindowListCopyWindowInfo` returns windows front-to-back, so the first layer-0 window for the active PID is the only truthful source for the current window title.
---