# Context Snapshot — US-003

- Task statement: Implement US-003 "Swift bridge for ScreenCaptureKit screenshots" from `.ralph/prd.json`.
- Desired outcome: Add a Swift screenshot bridge, Rust FFI wrapper, build integration, and a `mock-capture` test path that produces synthetic JPEG files without macOS screen-recording permissions.
- Known facts/evidence:
  - Highest-priority incomplete story is US-003 (priority 3, `passes: false`).
  - Current branch is `ralph/v1-implementation`, matching PRD `branchName`.
  - `src/capture/screenshot.rs` and `src/capture/window.rs` are currently empty stubs.
  - `Cargo.toml` already defines `mock-capture` feature but no implementation or Swift/build integration exists yet.
  - `SPEC.md` requires ScreenCaptureKit screenshots saved as JPEG with configurable quality and a thin Swift bridge.
  - `.factory/library/architecture.md` was requested by instructions but is absent from the repository.
- Constraints:
  - macOS-only design; use Apple APIs directly.
  - Capture layer must remain offline.
  - Implement only US-003 in this iteration.
  - Verification must include `cargo check`, `cargo clippy`, and `cargo test --features mock-capture`.
- Unknowns/open questions:
  - Best build integration approach for compiling Swift into a linkable library from Cargo on this machine.
  - Whether the native ScreenCaptureKit path can be made to compile on this host SDK without runtime permissions.
- Likely codebase touchpoints:
  - `Cargo.toml`
  - `build.rs`
  - `src/capture/screenshot.rs`
  - `src/capture/mod.rs`
  - new `swift/Sources/ScreenCapture.swift`
  - new `swift/Sources/bridge.h`
  - `AGENTS.md`, `.ralph/prd.json`, `.ralph/progress.txt`, `.pi/state/pi-ralph.json`
